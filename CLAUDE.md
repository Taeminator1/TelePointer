# TelePointer

## 작업 규칙

- 작업을 한 번에 몰아서 진행하지 않는다 — 한 단계를 끝내면 확인을 받고 다음으로 넘어간다
- [Tasks.md](Docs/v1/Tasks.md)의 항목도 하나씩 진행한다

## 커밋 규칙

- 커밋 전에 사용자의 코드 검토를 거친다 — 검토 없이 커밋하지 않는다
- 제목은 `<type>: <한국어 요약>` — Conventional Commits 타입 + 한국어 본문
- **부연 설명이 필요하다면** 제목 다음 빈 줄, 그 뒤에 `- ` 불릿으로 작성
    - 불릿은 꼭 필요할 때만 — 제목과 diff로 알 수 있는 내용은 쓰지 않는다
    - 바꾼 파일·항목을 나열하지 말고, 코드만 봐서는 알 수 없는 결정 근거만 남긴다
    - 제목 한 줄로 충분하면 본문 없이 끝낸다

## 주석 규칙

- 코드를 읽으면 알 수 있는 내용은 주석으로 쓰지 않는다
- 배경·의도·결정 근거는 `Docs/`에 두고 코드에는 남기지 않는다
- 남은 작업을 표시하는 `TODO` 마커는 예외
